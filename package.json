{
  "scripts": {
    "start": "rm -rf dist && webpack && http-server",
    "document": "rm -f doc/dependencies.svg && mkdir -p doc && madge src/index.ts -i doc/dependencies.svg"
  },
  "devDependencies": {
    "madge": "^4.0.1",
    "ts-loader": "^5.4.3",
    "typescript": "^3.4.5",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.1"
  }
}
